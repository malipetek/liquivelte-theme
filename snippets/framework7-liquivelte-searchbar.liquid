	{%- liquid
			
			
			assign classes = null
			assign noShadow = null
			assign noHairline = null
			assign form = null
			assign placeholder = null
			assign autocomplete = null
			assign autocorrect = null
			assign autocapitalize = null
			assign spellcheck = null
			assign disableButton = null
			assign disableButtonText = null
			assign clearButton = null
			assign value = null
			assign inputEvents = null
			assign expandable = null
			assign inline = null
			assign searchContainer = null
			assign searchIn = null
			assign searchItem = null
			assign searchGroup = null
			assign searchGroupTitle = null
			assign foundEl = null
			assign notFoundEl = null
			assign backdrop = null
			assign backdropEl = null
			assign hideOnEnableEl = null
			assign hideOnSearchEl = null
			assign ignore = null
			assign customSearch = null
			assign removeDiacritics = null
			assign hideDividers = null
			assign hideGroups = null
			assign init = null
			assign f7Slot = null
			-%}
	{%- liquid
	assign lv_props_arr = props | split: '-prsp-'
	for lv_prop in lv_props_arr
	assign lv_prop_and_value = lv_prop | split: '-kvsp-'
	assign lv_propName = lv_prop_and_value | first
	if lv_prop_and_value.size > 1
		assign lv_propValue = lv_prop_and_value | last | remove: '"'
	else
		assign lv_propValue = ''	
	endif
	if lv_propValue == '0'
		assign lv_propValue = lv_propValue | times: 1
	endif
	if lv_propValue contains '{' and lv_propValue contains '}'
	assign lv_propValue = lv_propValue | remove: '{' | remove: '}'
	assign lv_propValueArr = lv_propValue | split: ','
	for lv_entry in lv_propValueArr
	assign lv_key = lv_entry | split: ':' | first | strip | remove: '"'
	assign lv_value = lv_entry | split: ':' | last | strip | remove: '"'
	assign composite_key = lv_propName | append: '_' | append: lv_key
	
	endfor
	endif
	if lv_propName == 'classes' and classes == blank
		assign classes = lv_propValue
	endif
	if lv_propName == 'noShadow' and noShadow == blank
		assign noShadow = lv_propValue
	endif
	if lv_propName == 'noHairline' and noHairline == blank
		assign noHairline = lv_propValue
	endif
	if lv_propName == 'form' and form == blank
		assign form = lv_propValue
	endif
	if lv_propName == 'placeholder' and placeholder == blank
		assign placeholder = lv_propValue
	endif
	if lv_propName == 'autocomplete' and autocomplete == blank
		assign autocomplete = lv_propValue
	endif
	if lv_propName == 'autocorrect' and autocorrect == blank
		assign autocorrect = lv_propValue
	endif
	if lv_propName == 'autocapitalize' and autocapitalize == blank
		assign autocapitalize = lv_propValue
	endif
	if lv_propName == 'spellcheck' and spellcheck == blank
		assign spellcheck = lv_propValue
	endif
	if lv_propName == 'disableButton' and disableButton == blank
		assign disableButton = lv_propValue
	endif
	if lv_propName == 'disableButtonText' and disableButtonText == blank
		assign disableButtonText = lv_propValue
	endif
	if lv_propName == 'clearButton' and clearButton == blank
		assign clearButton = lv_propValue
	endif
	if lv_propName == 'value' and value == blank
		assign value = lv_propValue
	endif
	if lv_propName == 'inputEvents' and inputEvents == blank
		assign inputEvents = lv_propValue
	endif
	if lv_propName == 'expandable' and expandable == blank
		assign expandable = lv_propValue
	endif
	if lv_propName == 'inline' and inline == blank
		assign inline = lv_propValue
	endif
	if lv_propName == 'searchContainer' and searchContainer == blank
		assign searchContainer = lv_propValue
	endif
	if lv_propName == 'searchIn' and searchIn == blank
		assign searchIn = lv_propValue
	endif
	if lv_propName == 'searchItem' and searchItem == blank
		assign searchItem = lv_propValue
	endif
	if lv_propName == 'searchGroup' and searchGroup == blank
		assign searchGroup = lv_propValue
	endif
	if lv_propName == 'searchGroupTitle' and searchGroupTitle == blank
		assign searchGroupTitle = lv_propValue
	endif
	if lv_propName == 'foundEl' and foundEl == blank
		assign foundEl = lv_propValue
	endif
	if lv_propName == 'notFoundEl' and notFoundEl == blank
		assign notFoundEl = lv_propValue
	endif
	if lv_propName == 'backdrop' and backdrop == blank
		assign backdrop = lv_propValue
	endif
	if lv_propName == 'backdropEl' and backdropEl == blank
		assign backdropEl = lv_propValue
	endif
	if lv_propName == 'hideOnEnableEl' and hideOnEnableEl == blank
		assign hideOnEnableEl = lv_propValue
	endif
	if lv_propName == 'hideOnSearchEl' and hideOnSearchEl == blank
		assign hideOnSearchEl = lv_propValue
	endif
	if lv_propName == 'ignore' and ignore == blank
		assign ignore = lv_propValue
	endif
	if lv_propName == 'customSearch' and customSearch == blank
		assign customSearch = lv_propValue
	endif
	if lv_propName == 'removeDiacritics' and removeDiacritics == blank
		assign removeDiacritics = lv_propValue
	endif
	if lv_propName == 'hideDividers' and hideDividers == blank
		assign hideDividers = lv_propValue
	endif
	if lv_propName == 'hideGroups' and hideGroups == blank
		assign hideGroups = lv_propValue
	endif
	if lv_propName == 'init' and init == blank
		assign init = lv_propValue
	endif
	if lv_propName == 'f7Slot' and f7Slot == blank
		assign f7Slot = lv_propValue
	endif
	endfor
	-%}
	
			{%- liquid
				assign slot_contents_and_values = slot_contents | split: '-scs-'
				for content_and_value in slot_contents_and_values
					assign module_and_slotname = content_and_value | split: '-scvs-' | first
					assign lv_module = module_and_slotname | split: '-smns-' | first
					assign lv_name = module_and_slotname | split: '-smns-' | last
					assign lv_value = content_and_value | split: '-scvs-' | last
					
					if lv_module == 'framework7-liquivelte-searchbar'
						assign slot_default_framework7-liquivelte_searchbar = lv_value | strip
					endif
				endfor
				-%}
	{%- unless sub_include -%} 
	<div class="liquivelte-component searchbar" data-liquivelte-component-name="searchbar.liquivelte">
		{% assign component_include_count_slot_offset = 0 %}
		{% assign component_include_count = 0 %}
		{% assign component_include_depth = 0 %}
		<script> window.cicR = 0; </script>
	{%- endunless -%}
	

{% if form %}
  
    
		{%- if slot_default_framework7-liquivelte_searchbar != blank -%}
			{{- slot_default_framework7-liquivelte_searchbar -}}
		{%- endif -%}
	
    <div class="searchbar-inner">
      
		{%- if slot_default_framework7-liquivelte_searchbar != blank -%}
			{{- slot_default_framework7-liquivelte_searchbar -}}
		{%- endif -%}
	
      <div class="searchbar-input-wrap">
        
		{%- if slot_default_framework7-liquivelte_searchbar != blank -%}
			{{- slot_default_framework7-liquivelte_searchbar -}}
		{%- endif -%}
	
        <input
          value="{{ value | default: '' }}" liquivelte-value-cache="default§{{value | escape -}}, {{''}}§{{value| default: ''  }}"
          placeholder="{{ placeholder | default: '' }}" liquivelte-value-cache="default§{{placeholder | escape -}}, {{''}}§{{placeholder| default: ''  }}"
          autocomplete="{autocomplete}"
          autocorrect="{autocorrect}"
          autocapitalize="{autocapitalize}"
          spellcheck="{spellcheck}"
          type="search"
          on:input="{onInput}"
          on:change="{onChange}"
          on:focus="{onFocus}"
          on:blur="{onBlur}"
        />
        <i class="searchbar-icon" />
        {% if clearButton %}<span on:click="{onClearButtonClick}" class="input-clear-button" ></span>{% endif %}
        
		{%- if slot_default_framework7-liquivelte_searchbar != blank -%}
			{{- slot_default_framework7-liquivelte_searchbar -}}
		{%- endif -%}
	
      </div>
      {% if disableButton %}
        <span on:click="{onDisableButtonClick}" class="searchbar-disable-button"
          >{{ disableButtonText }}</span>
      {% endif %}
      
		{%- if slot_default_framework7-liquivelte_searchbar != blank -%}
			{{- slot_default_framework7-liquivelte_searchbar -}}
		{%- endif -%}
	
      
		{%- if slot_default_framework7-liquivelte_searchbar != blank -%}
			{{- slot_default_framework7-liquivelte_searchbar -}}
		{%- endif -%}
	
    </div>
    
		{%- if slot_default_framework7-liquivelte_searchbar != blank -%}
			{{- slot_default_framework7-liquivelte_searchbar -}}
		{%- endif -%}
	
  </form>
{% else %}
  <div bind:this="{el}" class="searchbar {{ classes }} {{ computedClasses }}" data-f7-slot="{f7Slot}" {...restProps($$restProps)}>
    
		{%- if slot_default_framework7-liquivelte_searchbar != blank -%}
			{{- slot_default_framework7-liquivelte_searchbar -}}
		{%- endif -%}
	
    <div class="searchbar-inner">
      
		{%- if slot_default_framework7-liquivelte_searchbar != blank -%}
			{{- slot_default_framework7-liquivelte_searchbar -}}
		{%- endif -%}
	
      <div class="searchbar-input-wrap">
        
		{%- if slot_default_framework7-liquivelte_searchbar != blank -%}
			{{- slot_default_framework7-liquivelte_searchbar -}}
		{%- endif -%}
	
        <input
          value="{{ value | default: '' }}" liquivelte-value-cache="default§{{value | escape -}}, {{''}}§{{value| default: ''  }}"
          placeholder="{{ placeholder | default: '' }}" liquivelte-value-cache="default§{{placeholder | escape -}}, {{''}}§{{placeholder| default: ''  }}"
          autocomplete="{autocomplete}"
          autocorrect="{autocorrect}"
          autocapitalize="{autocapitalize}"
          spellcheck="{spellcheck}"
          type="search"
          on:input="{onInput}"
          on:change="{onChange}"
          on:focus="{onFocus}"
          on:blur="{onBlur}"
        />
        <i class="searchbar-icon" />
        {% if clearButton %}<span on:click="{onClearButtonClick}" class="input-clear-button" ></span>{% endif %}
        
		{%- if slot_default_framework7-liquivelte_searchbar != blank -%}
			{{- slot_default_framework7-liquivelte_searchbar -}}
		{%- endif -%}
	
      </div>
      {% if disableButton %}
        <span on:click="{onDisableButtonClick}" class="searchbar-disable-button"
          >{{ disableButtonText }}</span>
      {% endif %}
      
		{%- if slot_default_framework7-liquivelte_searchbar != blank -%}
			{{- slot_default_framework7-liquivelte_searchbar -}}
		{%- endif -%}
	
      
		{%- if slot_default_framework7-liquivelte_searchbar != blank -%}
			{{- slot_default_framework7-liquivelte_searchbar -}}
		{%- endif -%}
	
    </div>
    
		{%- if slot_default_framework7-liquivelte_searchbar != blank -%}
			{{- slot_default_framework7-liquivelte_searchbar -}}
		{%- endif -%}
	
  </div>
{% endif %}

	
						
		
	<script type="text/noscript" class="instance-data" >
		{% assign component_include_count = component_include_count | plus: 1 %}
		{
	
	
	
		
		"forloop_index": {{ forloop.index0 | default: 0 | json }},
		"component_index": {{ component_include_count | plus: component_include_depth }},
		"component_name": "framework7-liquivelte-searchbar"
	}</script>
	
	<script class="liquivelte-prop-script">(() => {
	var propScript = Shopify.designMode && window.propScriptForDesignMode ? window.propScriptForDesignMode : document.currentScript;
	function parseProps(e){const s={};let t={bracketsOpened:0,get open(){return this.bracketsOpened>0},set open(e){!0===e?this.bracketsOpened=this.bracketsOpened+1:!1===e&&(this.bracketsOpened=this.bracketsOpened-1)}},a=` ${e} `.split(""),r=!1,c=!1,n=!1,p=!1,o="",b="";for(let e=0;e<a.length;e++){const k=a[e];switch(!0){case" "===k:p||t.open||(c=!1),!n||p||t.open||(n=!1);break;case"{"===k:t.open=!0,n||(c=!0);break;case"}"===k:t.open=!1;break;case'"'===k:p=!p;break;case"="===k:c&&(c=!1,n=!0,r=!0);break;case/[^\s]/.test(k):n||(c=!0)}!c||n||r?c||!n||r?c||n||(o&&(s[o]=b.replace(/^"/,"").replace(/"$/,"")),b="",o=""):b+=k:o+=k,r=!1}return Object.keys(s).map((e=>{if(/\{\s*\.\.\.(\w+)\s*\}/.test(e)){const[,t]=e.match(/\{\s*\.\.\.(\w+)\s*\}/);s.spread=t,delete s[e]}})),s}
	function JSONparse(n){try{return JSON.parse(n)}catch(t){try{const e=parseInt(t.message.match(/position\s+(\d+)/)[1],10),s=n.slice(0,e+1).split("\n"),i=s.length,l=n.split("\n"),r=s[s.length-1].length-1,c=l.slice(0,i).join("\n")+"\n"+new Array(r).fill(" ").join("")+"👆\n"+l.slice(i).join("\n");return console.log(c),{}}catch(n){throw t}}}
	function htmlDecode(input) {
	var doc = new DOMParser().parseFromString(input, "text/html");
	return doc.documentElement.textContent;
	}
	function componentWrapper(el) {
	if(el.classList.contains('liquivelte-component')) {
	return el;
	} else {
	return componentWrapper(el.parentElement);
	}
	}
	var wrapper = componentWrapper(propScript.parentElement);
	// console.log('wrapper', wrapper);
	// console.log('parsing props for svelte component');
	try{
		const themeImports = (JSONparse(propScript.previousElementSibling.textContent));
		wrapper.svelteProps = wrapper.svelteProps || {};
		Object.keys(themeImports).forEach(key => {
			if(key == 'forloop_index' || key == 'component_index' || key == 'component_name') return;
			wrapper.svelteProps[key] = [
				...(wrapper.svelteProps[key] || []),
				{ forloop_index: themeImports.index, component_index: themeImports.component_index, component_name: themeImports.component_name, value: themeImports[key] }
			];
		});
	} catch (e) {
	const err = 'There was an error';
	console.error('there was an error parsing props json', e);
	wrapper.svelteProps = { ...wrapper.svelteProps, error: (wrapper.svelteProps.error || '') + err};
	}
	
	

	// liquid expression cache
	[...wrapper.querySelectorAll('[liquivelte-value-cache]')].forEach(el => {
	let [filter, args, value] = el.getAttribute('liquivelte-value-cache').split('§');
	args = args.split(', ');
	args[0] = args[0].replace(/^"|\\|"$/g, '');
	wrapper.liquid_expression_cache = wrapper.liquid_expression_cache || {};
	wrapper.liquid_expression_cache[filter] = wrapper.liquid_expression_cache[filter] || new Map;
	wrapper.liquid_expression_cache[filter].set(`${args.join(',')}`, value);
	});

	if(propScript){
	propScript.remove();
	}
	if(propScript.previousElementSibling) {
	propScript.previousElementSibling.remove();
	}
	})();</script>
	{%- liquid
			
			
			assign classes = null
			assign noShadow = null
			assign noHairline = null
			assign form = null
			assign placeholder = null
			assign autocomplete = null
			assign autocorrect = null
			assign autocapitalize = null
			assign spellcheck = null
			assign disableButton = null
			assign disableButtonText = null
			assign clearButton = null
			assign value = null
			assign inputEvents = null
			assign expandable = null
			assign inline = null
			assign searchContainer = null
			assign searchIn = null
			assign searchItem = null
			assign searchGroup = null
			assign searchGroupTitle = null
			assign foundEl = null
			assign notFoundEl = null
			assign backdrop = null
			assign backdropEl = null
			assign hideOnEnableEl = null
			assign hideOnSearchEl = null
			assign ignore = null
			assign customSearch = null
			assign removeDiacritics = null
			assign hideDividers = null
			assign hideGroups = null
			assign init = null
			assign f7Slot = null
			-%}
	{%- liquid 
			assign slot_default_framework7-liquivelte_searchbar = null
			
			-%}
	
	{%- unless sub_include -%} 
	</div>
	{% assign component_include_count = 0 %}
	{%- endunless -%}
	