	{%- liquid
			
			
			assign classes = null
			assign backLink = null
			assign backLinkUrl = null
			assign backLinkForce = null
			assign backLinkShowText = null
			assign sliding = null
			assign title = null
			assign subtitle = null
			assign hidden = null
			assign noShadow = null
			assign noHairline = null
			assign innerClass = null
			assign innerClassName = null
			assign large = null
			assign largeTransparent = null
			assign transparent = null
			assign titleLarge = null
			assign f7Slot = null
			-%}
	{%- liquid
	assign lv_props_arr = props | split: '-prsp-'
	for lv_prop in lv_props_arr
	assign lv_prop_and_value = lv_prop | split: '-kvsp-'
	assign lv_propName = lv_prop_and_value | first
	if lv_prop_and_value.size > 1
		assign lv_propValue = lv_prop_and_value | last | remove: '"'
	else
		assign lv_propValue = ''	
	endif
	if lv_propValue == '0'
		assign lv_propValue = lv_propValue | times: 1
	endif
	if lv_propValue contains '{' and lv_propValue contains '}'
	assign lv_propValue = lv_propValue | remove: '{' | remove: '}'
	assign lv_propValueArr = lv_propValue | split: ','
	for lv_entry in lv_propValueArr
	assign lv_key = lv_entry | split: ':' | first | strip | remove: '"'
	assign lv_value = lv_entry | split: ':' | last | strip | remove: '"'
	assign composite_key = lv_propName | append: '_' | append: lv_key
	
	endfor
	endif
	if lv_propName == 'classes' and classes == blank
		assign classes = lv_propValue
	endif
	if lv_propName == 'backLink' and backLink == blank
		assign backLink = lv_propValue
	endif
	if lv_propName == 'backLinkUrl' and backLinkUrl == blank
		assign backLinkUrl = lv_propValue
	endif
	if lv_propName == 'backLinkForce' and backLinkForce == blank
		assign backLinkForce = lv_propValue
	endif
	if lv_propName == 'backLinkShowText' and backLinkShowText == blank
		assign backLinkShowText = lv_propValue
	endif
	if lv_propName == 'sliding' and sliding == blank
		assign sliding = lv_propValue
	endif
	if lv_propName == 'title' and title == blank
		assign title = lv_propValue
	endif
	if lv_propName == 'subtitle' and subtitle == blank
		assign subtitle = lv_propValue
	endif
	if lv_propName == 'hidden' and hidden == blank
		assign hidden = lv_propValue
	endif
	if lv_propName == 'noShadow' and noShadow == blank
		assign noShadow = lv_propValue
	endif
	if lv_propName == 'noHairline' and noHairline == blank
		assign noHairline = lv_propValue
	endif
	if lv_propName == 'innerClass' and innerClass == blank
		assign innerClass = lv_propValue
	endif
	if lv_propName == 'innerClassName' and innerClassName == blank
		assign innerClassName = lv_propValue
	endif
	if lv_propName == 'large' and large == blank
		assign large = lv_propValue
	endif
	if lv_propName == 'largeTransparent' and largeTransparent == blank
		assign largeTransparent = lv_propValue
	endif
	if lv_propName == 'transparent' and transparent == blank
		assign transparent = lv_propValue
	endif
	if lv_propName == 'titleLarge' and titleLarge == blank
		assign titleLarge = lv_propValue
	endif
	if lv_propName == 'f7Slot' and f7Slot == blank
		assign f7Slot = lv_propValue
	endif
	endfor
	-%}
	
			{%- liquid
				assign slot_contents_and_values = slot_contents | split: '-scs-'
				for content_and_value in slot_contents_and_values
					assign module_and_slotname = content_and_value | split: '-scvs-' | first
					assign lv_module = module_and_slotname | split: '-smns-' | first
					assign lv_name = module_and_slotname | split: '-smns-' | last
					assign lv_value = content_and_value | split: '-scvs-' | last
					if lv_module == 'framework7-liquivelte-navbar' and lv_name == 'before-inner'
						assign slot_before-inner_framework7-liquivelte_navbar = lv_value | strip
					endif
					if lv_module == 'framework7-liquivelte-navbar' and lv_name == 'nav-left'
						assign slot_nav-left_framework7-liquivelte_navbar = lv_value | strip
					endif
					if lv_module == 'framework7-liquivelte-navbar' and lv_name == 'left'
						assign slot_left_framework7-liquivelte_navbar = lv_value | strip
					endif
					if lv_module == 'framework7-liquivelte-navbar' and lv_name == 'title'
						assign slot_title_framework7-liquivelte_navbar = lv_value | strip
					endif
					if lv_module == 'framework7-liquivelte-navbar' and lv_name == 'nav-right'
						assign slot_nav-right_framework7-liquivelte_navbar = lv_value | strip
					endif
					if lv_module == 'framework7-liquivelte-navbar' and lv_name == 'right'
						assign slot_right_framework7-liquivelte_navbar = lv_value | strip
					endif
					if lv_module == 'framework7-liquivelte-navbar' and lv_name == 'title-large'
						assign slot_title-large_framework7-liquivelte_navbar = lv_value | strip
					endif
					if lv_module == 'framework7-liquivelte-navbar' and lv_name == 'after-inner'
						assign slot_after-inner_framework7-liquivelte_navbar = lv_value | strip
					endif
					
					if lv_module == 'framework7-liquivelte-navbar'
						assign slot_default_framework7-liquivelte_navbar = lv_value | strip
					endif
				endfor
				-%}
	{%- unless sub_include -%} 
	<div class="liquivelte-component navbar" data-liquivelte-component-name="navbar.liquivelte">
		{% assign component_include_count_slot_offset = 0 %}
		{% assign component_include_count = 0 %}
		{% assign component_include_depth = 0 %}
		<script> window.cicR = 0; </script>
	{%- endunless -%}
	

<div class="navbar {{ classes }} {{ computedClasses }}" bind:this="{el}" data-f7-slot="{f7Slot}" {...restProps($$restProps)}>
  <div class="navbar-bg" ></div>
  
	{%- if slot_before-inner_framework7-liquivelte_navbar != blank -%}
		{{- slot_before-inner_framework7-liquivelte_navbar -}}
	{%- endif -%}

  <div class="navbar-inner {{ innerClasses }}">
    {% if backLink or hasLeftSlots %}
      
{% comment %}
kvsp stands for "key value separator"
prsp stands for "props separator"
{% endcomment %}
{% comment %}
scs stands for "slot component separator"
scvs stands for "slot component value separator"
smns stands for "slot module name separator"
{% endcomment %}
{% capture props_NavLeft %}{backLink}-kvsp-1-prsp-{backLinkUrl}-kvsp-1-prsp-{backLinkForce}-kvsp-1-prsp-{backLinkShowText}-kvsp-1-prsp-{onBackClick}-kvsp-1{% endcapture %}
{%- assign component_include_count_before_slots = component_include_count -%}

{%- capture slot_content_def_NavLeft -%}{% assign component_include_depth = component_include_depth | plus: 1 %}
        
	{%- if slot_nav-left_framework7-liquivelte_navbar != blank -%}
		{{- slot_nav-left_framework7-liquivelte_navbar -}}
	{%- endif -%}

        
	{%- if slot_left_framework7-liquivelte_navbar != blank -%}
		{{- slot_left_framework7-liquivelte_navbar -}}
	{%- endif -%}

      {%- endcapture -%}{% assign component_include_depth = component_include_depth | minus: 1 %}
{%- assign component_include_count_slot_offset = component_include_count | minus: component_include_count_before_slots -%}
{% assign modulename = basename | append: 'nav-left' %}
{% assign slot_content_NavLeft = slot_content_NavLeft | append: '-scs-' | append: modulename | append: '-scvs-' | append: slot_content_def_NavLeft %}
{% include modulename, liquivelte: true, props: props_NavLeft, sub_include: true, slot_contents: slot_content_NavLeft, basename: ''  %}
{% assign slot_content_NavLeft = '' %}
{% assign props = '' %}
    {% endif %}
    {% if title or subtitle or hasTitleSlots %}
      
{% comment %}
kvsp stands for "key value separator"
prsp stands for "props separator"
{% endcomment %}
{% comment %}
scs stands for "slot component separator"
scvs stands for "slot component value separator"
smns stands for "slot module name separator"
{% endcomment %}
{% capture props_NavTitle %}{title}-kvsp-1-prsp-{subtitle}-kvsp-1{% endcapture %}
{%- assign component_include_count_before_slots = component_include_count -%}

{%- capture slot_content_def_NavTitle -%}{% assign component_include_depth = component_include_depth | plus: 1 %}
        
	{%- if slot_title_framework7-liquivelte_navbar != blank -%}
		{{- slot_title_framework7-liquivelte_navbar -}}
	{%- endif -%}

      {%- endcapture -%}{% assign component_include_depth = component_include_depth | minus: 1 %}
{%- assign component_include_count_slot_offset = component_include_count | minus: component_include_count_before_slots -%}
{% assign modulename = basename | append: 'nav-title' %}
{% assign slot_content_NavTitle = slot_content_NavTitle | append: '-scs-' | append: modulename | append: '-scvs-' | append: slot_content_def_NavTitle %}
{% include modulename, liquivelte: true, props: props_NavTitle, sub_include: true, slot_contents: slot_content_NavTitle, basename: ''  %}
{% assign slot_content_NavTitle = '' %}
{% assign props = '' %}
    {% endif %}
    {% if hasRightSlots %}
      
{% comment %}
kvsp stands for "key value separator"
prsp stands for "props separator"
{% endcomment %}
{% comment %}
scs stands for "slot component separator"
scvs stands for "slot component value separator"
smns stands for "slot module name separator"
{% endcomment %}
{% capture props_NavRight %}undefined-kvsp-1{% endcapture %}
{%- assign component_include_count_before_slots = component_include_count -%}

{%- capture slot_content_def_NavRight -%}{% assign component_include_depth = component_include_depth | plus: 1 %}
        
	{%- if slot_nav-right_framework7-liquivelte_navbar != blank -%}
		{{- slot_nav-right_framework7-liquivelte_navbar -}}
	{%- endif -%}

        
	{%- if slot_right_framework7-liquivelte_navbar != blank -%}
		{{- slot_right_framework7-liquivelte_navbar -}}
	{%- endif -%}

      {%- endcapture -%}{% assign component_include_depth = component_include_depth | minus: 1 %}
{%- assign component_include_count_slot_offset = component_include_count | minus: component_include_count_before_slots -%}
{% assign modulename = basename | append: 'nav-right' %}
{% assign slot_content_NavRight = slot_content_NavRight | append: '-scs-' | append: modulename | append: '-scvs-' | append: slot_content_def_NavRight %}
{% include modulename, liquivelte: true, props: props_NavRight, sub_include: true, slot_contents: slot_content_NavRight, basename: ''  %}
{% assign slot_content_NavRight = '' %}
{% assign props = '' %}
    {% endif %}
    {% if largeTitle or hasTitleLargeSlots %}
      <div class="title-large">
        <div class="title-large-text">
          {{ largeTitle | default: '' }}
          
	{%- if slot_title-large_framework7-liquivelte_navbar != blank -%}
		{{- slot_title-large_framework7-liquivelte_navbar -}}
	{%- endif -%}

        </div>
      </div>
    {% endif %}
    
		{%- if slot_default_framework7-liquivelte_navbar != blank -%}
			{{- slot_default_framework7-liquivelte_navbar -}}
		{%- endif -%}
	
  </div>
  
	{%- if slot_after-inner_framework7-liquivelte_navbar != blank -%}
		{{- slot_after-inner_framework7-liquivelte_navbar -}}
	{%- endif -%}

</div>

	
						
		
	<script type="text/noscript" class="instance-data" >
		{% assign component_include_count = component_include_count | plus: 1 %}
		{
	
	
	
		
		"forloop_index": {{ forloop.index0 | default: 0 | json }},
		"component_index": {{ component_include_count | plus: component_include_depth }},
		"component_name": "framework7-liquivelte-navbar"
	}</script>
	
	<script class="liquivelte-prop-script">(() => {
	var propScript = Shopify.designMode && window.propScriptForDesignMode ? window.propScriptForDesignMode : document.currentScript;
	function parseProps(e){const s={};let t={bracketsOpened:0,get open(){return this.bracketsOpened>0},set open(e){!0===e?this.bracketsOpened=this.bracketsOpened+1:!1===e&&(this.bracketsOpened=this.bracketsOpened-1)}},a=` ${e} `.split(""),r=!1,c=!1,n=!1,p=!1,o="",b="";for(let e=0;e<a.length;e++){const k=a[e];switch(!0){case" "===k:p||t.open||(c=!1),!n||p||t.open||(n=!1);break;case"{"===k:t.open=!0,n||(c=!0);break;case"}"===k:t.open=!1;break;case'"'===k:p=!p;break;case"="===k:c&&(c=!1,n=!0,r=!0);break;case/[^\s]/.test(k):n||(c=!0)}!c||n||r?c||!n||r?c||n||(o&&(s[o]=b.replace(/^"/,"").replace(/"$/,"")),b="",o=""):b+=k:o+=k,r=!1}return Object.keys(s).map((e=>{if(/\{\s*\.\.\.(\w+)\s*\}/.test(e)){const[,t]=e.match(/\{\s*\.\.\.(\w+)\s*\}/);s.spread=t,delete s[e]}})),s}
	function JSONparse(n){try{return JSON.parse(n)}catch(t){try{const e=parseInt(t.message.match(/position\s+(\d+)/)[1],10),s=n.slice(0,e+1).split("\n"),i=s.length,l=n.split("\n"),r=s[s.length-1].length-1,c=l.slice(0,i).join("\n")+"\n"+new Array(r).fill(" ").join("")+"👆\n"+l.slice(i).join("\n");return console.log(c),{}}catch(n){throw t}}}
	function htmlDecode(input) {
	var doc = new DOMParser().parseFromString(input, "text/html");
	return doc.documentElement.textContent;
	}
	function componentWrapper(el) {
	if(el.classList.contains('liquivelte-component')) {
	return el;
	} else {
	return componentWrapper(el.parentElement);
	}
	}
	var wrapper = componentWrapper(propScript.parentElement);
	// console.log('wrapper', wrapper);
	// console.log('parsing props for svelte component');
	try{
		const themeImports = (JSONparse(propScript.previousElementSibling.textContent));
		wrapper.svelteProps = wrapper.svelteProps || {};
		Object.keys(themeImports).forEach(key => {
			if(key == 'forloop_index' || key == 'component_index' || key == 'component_name') return;
			wrapper.svelteProps[key] = [
				...(wrapper.svelteProps[key] || []),
				{ forloop_index: themeImports.index, component_index: themeImports.component_index, component_name: themeImports.component_name, value: themeImports[key] }
			];
		});
	} catch (e) {
	const err = 'There was an error';
	console.error('there was an error parsing props json', e);
	wrapper.svelteProps = { ...wrapper.svelteProps, error: (wrapper.svelteProps.error || '') + err};
	}
	
	

	// liquid expression cache
	[...wrapper.querySelectorAll('[liquivelte-value-cache]')].forEach(el => {
	let [filter, args, value] = el.getAttribute('liquivelte-value-cache').split('§');
	args = args.split(', ');
	args[0] = args[0].replace(/^"|\\|"$/g, '');
	wrapper.liquid_expression_cache = wrapper.liquid_expression_cache || {};
	wrapper.liquid_expression_cache[filter] = wrapper.liquid_expression_cache[filter] || new Map;
	wrapper.liquid_expression_cache[filter].set(`${args.join(',')}`, value);
	});

	if(propScript){
	propScript.remove();
	}
	if(propScript.previousElementSibling) {
	propScript.previousElementSibling.remove();
	}
	})();</script>
	{%- liquid
			
			
			assign classes = null
			assign backLink = null
			assign backLinkUrl = null
			assign backLinkForce = null
			assign backLinkShowText = null
			assign sliding = null
			assign title = null
			assign subtitle = null
			assign hidden = null
			assign noShadow = null
			assign noHairline = null
			assign innerClass = null
			assign innerClassName = null
			assign large = null
			assign largeTransparent = null
			assign transparent = null
			assign titleLarge = null
			assign f7Slot = null
			-%}
	{%- liquid 
			assign slot_default_framework7-liquivelte_navbar = null
			assign slot_before-inner_framework7-liquivelte_navbar = null
			assign slot_nav-left_framework7-liquivelte_navbar = null
			assign slot_left_framework7-liquivelte_navbar = null
			assign slot_title_framework7-liquivelte_navbar = null
			assign slot_nav-right_framework7-liquivelte_navbar = null
			assign slot_right_framework7-liquivelte_navbar = null
			assign slot_title-large_framework7-liquivelte_navbar = null
			assign slot_after-inner_framework7-liquivelte_navbar = null
			
			-%}
	
	{%- unless sub_include -%} 
	</div>
	{% assign component_include_count = 0 %}
	{%- endunless -%}
	