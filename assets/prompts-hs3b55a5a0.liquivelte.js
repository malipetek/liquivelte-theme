import { element, claim_element, children, detach, attr, insert_hydration, noop, onMount, SvelteComponent, init, safe_not_equal } from './liquivelte-svelte-hs623f7d73.liquivelte.js';
import { cachedLiquid } from './liquivelte-liquid-hs6dbc6cca.liquivelte.js';
import { f7ready, f7 } from './header-hs533fc9cb.liquivelte.js';
import './store.js-hs80deeb5c.liquivelte.js';

/* Usersmalipetek/Documents/Documents/Projects/LIQUVELTE/LIQUIVELTE TEST/src/sections/prompts/index.liquivelte generated by Svelte v3.50.0 */

function create_fragment(ctx) {
	let div;

	return {
		c() {
			div = element("div");
			this.h();
		},
		l(nodes) {
			div = claim_element(nodes, "DIV", { prompts: true });
			var div_nodes = children(div);
			div_nodes.forEach(detach);
			this.h();
		},
		h() {
			attr(div, "prompts", "");
		},
		m(target, anchor) {
			insert_hydration(target, div, anchor);
		},
		p: noop,
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(div);
		}
	};
}

let index = 0;

function instance($$self, $$props, $$invalidate) {
	let { lec } = $$props;
	const liquid = cachedLiquid(lec);
	let notification;

	onMount(() => {
		f7ready(() => {
			notification = f7.notification.create({
				title: 'Newsletter',
				titleRightText: 'now',
				subtitle: 'Subscribe to newsletter to get 20% off',
				text: 'Tap on this to subscribe.',
				closeTimeout: 3000
			});

			setTimeout(
				() => {
					notification.open();
				},
				2000
			);
		});
	});

	$$self.$$set = $$props => {
		if ('lec' in $$props) $$invalidate(0, lec = $$props.lec);
	};

	return [lec];
}

class Prompts extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, { lec: 0 });
	}
}

export { Prompts as default };
